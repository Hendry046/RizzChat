<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Selected tab -->
    <item android:drawable="@color/selected_tab_color" android:state_selected="true"/>
    <!-- Pressed tab -->
    <item android:drawable="@color/pressed_tab_color" android:state_pressed="true"/>
    <!-- Default tab -->
    <item android:drawable="@android:color/transparent"/>


</selector>